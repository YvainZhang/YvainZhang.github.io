# 03 权重在线解压缩硬件引擎

## 1. 权重带宽压缩技术 (Weight Decompression)

为了降低外部 DDR/HBM 访存带宽消耗，模型权重在离线编译期被压缩（如采用无损 Huffman 编码或稀疏 Run-length 编码）：
- **硬件解压引擎**：集成在 Tensor DMA 接收端，边从总线接收压缩码流，边实时解压还原为原始浮点/定点张量写入片上 SRAM，使外部带宽利用率等效提升 1.5~2.5 倍。
