# 04 AI 编译器工程调优与规避指南

## 1. 编译器工程调优常见问题

| 调优现象 | 根因诊断 | 解决方案 |
| :--- | :--- | :--- |
| **模型编译后执行时间远慢于预期** | Tiling 尺寸选择不当，导致 DMA 搬运时间远大于计算时间（访存受限） | 采用自动调优（Auto-tuning）搜索最佳 $T_m, T_n, T_k$ 组合 |
| **SRAM 编译期分配溢出 (OOM)** | 多个并行算子生命周期重叠，超出 SRAM 物理上限 | 引入静态内存溢出 Spill-to-DDR 降级机制 |
