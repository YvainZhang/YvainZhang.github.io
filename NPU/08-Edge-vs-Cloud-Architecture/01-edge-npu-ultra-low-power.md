# 01 端侧 NPU 架构：极致低功耗与 ISP 深度融合

## 1. 端侧 NPU 核心特征

- **功耗预算**：通常在 0.5W ~ 5W 之间（手机、IPC 摄像头、无人机、车机座舱）。
- **内存形态**：无独立显存，与 SoC CPU/GPU 共享低功耗 LPDDR4X/LPDDR5。
- **与 ISP 零拷贝融合**：摄像头传感器原始 Bayer 数据通过 MIPI 接口进入 ISP，ISP 硬件处理后直接将 YUV 特征图输出至 NPU 共享内存，直接触发目标检测模型。
